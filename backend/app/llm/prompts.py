"""
LLM提示词模板
定义用于Markdown优化、乱码修复和图片OCR的提示词
"""

# Markdown优化提示词
OPTIMIZE_PROMPT = """你是一个专业的文档格式优化助手。你的任务是将基础转换得到的Markdown文本进行优化和修复。

## 优化要求

1. **格式修复**：
   - 修复错误的Markdown语法
   - 确保标题层级正确（H1 > H2 > H3...）
   - 修复表格格式，确保列对齐
   - 修复列表缩进

2. **结构补充**：
   - 如果文档缺少标题，根据内容添加合适的标题
   - 将长段落适当分段
   - 为关键内容添加加粗或引用标记

3. **内容一致性**：
   - 严格保持原始内容不变，不要添加、删除或修改任何实际内容
   - 不要编造或推测原文中没有的信息
   - 保持专业术语、人名、数字等原样不变

4. **输出要求**：
   - 只输出优化后的Markdown文本
   - 不要添加任何解释性说明
   - 不要用代码块包裹输出

## 原始文件类型
{file_type}

## 基础转换结果
{converted_markdown}
"""

# 乱码修复提示词
FIX_GARBLED_PROMPT = """以下是从{file_type}文件中提取的文本，由于字体编码问题导致中文出现乱码。
请你根据上下文和残存的可读信息，推断并还原出正确的中文内容。

## 修复要求
1. 根据上下文推断乱码部分的正确中文内容
2. 保持文档的原始结构和格式
3. 数字、英文、标点符号通常是正确的，保留不变
4. 如果无法确定某个乱码的具体内容，根据上下文合理推测
5. 输出完整的修复后Markdown文档，不要添加解释说明

## 乱码文本
{garbled_text}
"""

# 图片OCR识别提示词
IMAGE_OCR_PROMPT = """请识别这张图片中的所有文字内容，并将其转换为结构清晰的Markdown格式。

## 识别要求
1. 准确识别所有文字，包括标题、正文、列表、表格等
2. 保持原文档的层级结构，用Markdown标题表示
3. 表格内容用Markdown表格格式输出
4. 数学公式用LaTeX语法表示
5. 代码内容用代码块包裹
6. 不要添加原文中没有的内容
7. 这是第{page_num}页的内容

请直接输出Markdown格式的识别结果，不要添加任何解释。
"""
